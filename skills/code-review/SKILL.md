---
name: code-review
description: 在合并前做结构化代码审查：正确性、边界、安全、性能、可维护性。用户要 review、审查、挑刺时触发。
---

1. `edit_local_file` 读取相关文件（mode=r）；若有 PR diff，优先从终端 `git diff` 获取。
2. 输出：**摘要** → **必修复** → **建议** → **可选优化**；安全项（密钥、注入、命令拼接）单列。
3. 对每条问题给出文件路径与具体行号或符号名（若可读）。
4. 不做无根据的性能断言；缺测试时明确说「未验证」。
