---
name: demo-plugin-skill
description: Example skill shipped via plugins/demo + openclaw.plugin.json (lowest precedence vs workspace skills).
metadata:
  openclaw:
    always: true
---

1. Demonstrates **plugin → skills/** discovery.
2. Real workflows should live in workspace `skills/` to override bundled names.
